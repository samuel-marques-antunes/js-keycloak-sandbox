{
  "name": "scripts",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npm run create-realm && node --test && npm run delete-realm",
    "create-realm": "node create-realm.js",
    "delete-realm": "node delete-realm.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@keycloak/keycloak-admin-client": "^26.1.4"
  }
}
